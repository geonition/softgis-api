The softGIS api provides django apps to set up a REST API
for saving and retrieval of geographical information.

The applications contain both javascript functions and
REST functionallity implemented on top of geodjango.

At the moment this project is developing in a fast pace and
you cannot expect it to be stable.
